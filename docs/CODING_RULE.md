# Coding Rules

## 必须遵守

1. 永远交付完整文件。
2. 不交付代码片段。
3. 每次开发完成执行：

flutter analyze

必须输出：

No issues found!

4. 一个 Sprint 完成后才能进入下一 Sprint。

5. 所有目录保持稳定。

6. 所有 import 使用 package 导入。

7. 所有 Widget 使用 const（可用时）。

8. 一个文件只负责一种职责。

9. 每个 Sprint 更新 docs。

10. Git 每个 Sprint 提交一次。